import { GreenElDirective } from './green-el.directive';

describe('GreenElDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenElDirective();
    expect(directive).toBeTruthy();
  });
});
